import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h3>This is uncle</h3>
            <p>House: {house}</p>
        </div>
    );
};

export default Uncle;